package com.atian10.logrecord.core.export;

/**
 * 导出格式枚举
 */
public enum ExportFormat {
    /** 纯文本格式（人类可读） */
    TXT,
    /** JSON 格式（结构化，便于程序解析） */
    JSON,
    /** CSV 格式（表格，便于 Excel 查看） */
    CSV
}
