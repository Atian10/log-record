# 日志记录库 - 架构设计文档

> 版本：v1.0
> 创建时间：2026-07-15
> 仓库地址：https://github.com/Atian10/log-record

---

## 一、架构总览

### 1.1 设计目标

- **跨平台**：一套核心代码支持 Android / Windows 桌面 / Windows 服务器 / Linux 服务器
- **可复用**：作为基础组件被其他项目引用，业务方无需重复造轮子
- **零平台依赖核心**：`log-core` 纯 Java SE 实现，所有平台共用
- **平台适配层**：平台相关能力（Room/JDBC/Logcat）通过适配模块注入

### 1.2 架构分层

```
┌─────────────────────────────────────────────────────────┐
│                  业务项目（使用方）                       │
│   调用 LogManager API / 查询日志 / 自行上报              │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    log-core (核心模块)                   │
│   纯 Java SE 实现，零平台依赖                            │
│   ┌──────────┬──────────┬──────────┬──────────┐        │
│   │  接口层  │  引擎层  │  查询层  │  清理层  │        │
│   └──────────┴──────────┴──────────┴──────────┘        │
└──┬───────────────────────────────────────────────────┬──┘
   │                                                   │
   │  依赖注入（平台适配）                              │
   │                                                   │
┌──▼──────────────────┐                ┌───────────────▼──┐
│  log-android        │                │  log-desktop     │
│  Android 平台适配   │                │  桌面/服务器适配  │
│  ┌────────────┐     │                │  ┌────────────┐  │
│  │LogcatStorage│    │                │  │ConsoleStorage│ │
│  │RoomStorage │     │                │  │JdbcStorage │  │
│  └────────────┘     │                │  └────────────┘  │
└─────────────────────┘                └──────────────────┘
```

---

## 二、模块划分

### 2.1 模块清单

| 模块 | 职责 | 依赖 | 平台 |
|------|------|------|------|
| `log-core` | 核心接口、引擎、查询、清理（纯 Java SE） | 无 | 全平台 |
| `log-android` | Android 平台适配（Room + Logcat） | log-core | Android |
| `log-desktop` | 桌面/服务器适配（JDBC SQLite + Console） | log-core | JVM |
| `log-sample` | 桌面/服务器使用示例（纯 Java） | log-desktop | JVM |
| `log-sample-android` | Android 应用示例 | log-android | Android |

### 2.2 模块依赖关系

```
log-sample (桌面/服务器示例)
  └── log-desktop
        └── log-core

log-sample-android (Android 示例)
  └── log-android
        └── log-core
```

**依赖规则**：
- `log-core` 不依赖任何平台模块（强制）
- 平台模块单向依赖 `log-core`
- 业务项目只依赖需要的平台模块

---

## 三、项目目录结构

```
log-record/
├── settings.gradle                              # 多模块配置
├── build.gradle                                 # 根构建脚本
├── gradle.properties                            # Gradle 属性
├── gradlew / gradlew.bat                        # Gradle Wrapper
├── README.md
├── .gitignore
│
├── docs/                                        # 项目文档
│   ├── 日志记录库-功能设计文档.md
│   └── 日志记录库-架构设计文档.md
│
├── log-core/                                    # 核心模块（纯 Java SE）
│   ├── build.gradle
│   ├── consumer-rules.pro                       # ProGuard keep 规则
│   └── src/
│       ├── main/
│       │   └── java/com/atian10/logrecord/core/
│       │       ├── ILogger.java                 # 日志写入接口
│       │       ├── ILoggerEngine.java           # 引擎接口
│       │       ├── IStorage.java                # 存储抽象接口
│       │       ├── IFormatter.java              # 格式化接口
│       │       ├── ILogFilter.java              # 过滤器接口
│       │       ├── IExceptionStorage.java       # 异常存储接口（独立）
│       │       │
│       │       ├── model/
│       │       │   ├── LogRecord.java           # 日志数据模型
│       │       │   ├── ExceptionRecord.java     # 异常数据模型
│       │       │   ├── LogLevel.java            # 日志级别枚举
│       │       │   └── StandardLogType.java     # 预设日志类型枚举
│       │       │
│       │       ├── config/
│       │       │   ├── LogConfig.java           # 配置类
│       │       │   ├── LogConfigUpdater.java    # 运行时配置更新器
│       │       │   └── CleanPolicy.java         # 清理策略配置
│       │       │
│       │       ├── engine/
│       │       │   ├── AsyncLoggerEngine.java   # 异步引擎实现
│       │       │   ├── LogStatus.java           # 库状态类（含 isNormal/isDegraded/isError/isShutdown）
│       │       │   ├── QueueFullPolicy.java     # 队列满策略枚举
│       │       │   └── LogEntry.java            # 队列包装类（区分 LogRecord/ExceptionRecord）
│       │       │
│       │       ├── query/
│       │       │   ├── LogQuery.java            # 日志查询条件
│       │       │   ├── ExceptionQuery.java      # 异常查询条件
│       │       │   ├── LogStatistics.java       # 聚合统计结果
│       │       │   └── OrderBy.java             # 排序枚举
│       │       │
│       │       ├── export/
│       │       │   ├── ExportFormat.java        # 导出格式枚举
│       │       │   ├── ExportEncoding.java      # 导出编码枚举
│       │       │   ├── ExportCallback.java      # 导出回调
│       │       │   └── Exporter.java            # 导出器实现
│       │       │
│       │       ├── formatter/
│       │       │   └── DefaultFormatter.java    # 默认格式化器
│       │       │
│       │       ├── clean/
│       │       │   ├── CleanCallback.java       # 清理回调
│       │       │   └── CleanTask.java           # 定时清理任务
│       │       │
│       │       ├── LogManager.java              # 全局入口（单例）
│       │       └── util/
│       │           ├── TimeUtil.java            # 时间工具
│       │           ├── JsonUtil.java            # JSON 工具
│       │           └── StackTraceUtil.java      # 堆栈工具
│       │
│       └── test/                                # 单元测试
│           └── java/com/atian10/logrecord/core/
│
├── log-android/                                 # Android 平台适配
│   ├── build.gradle
│   ├── consumer-rules.pro
│   └── src/
│       └── main/
│           ├── AndroidManifest.xml
│           └── java/com/atian10/logrecord/android/
│               ├── LogcatStorage.java           # Logcat 输出
│               ├── room/
│               │   ├── LogEntity.java           # Room Entity
│               │   ├── ExceptionEntity.java     # 异常 Entity
│               │   ├── LogDao.java              # 日志 DAO
│               │   ├── ExceptionDao.java        # 异常 DAO
│               │   ├── LogDatabase.java         # Room Database
│               │   └── LogDatabaseMigrations.java # 迁移
│               ├── RoomStorage.java             # Room 实现 IStorage
│               ├── RoomExceptionStorage.java    # Room 实现异常存储
│               └── AndroidLogInit.java          # Android 初始化入口
│
├── log-desktop/                                 # 桌面/服务器适配
│   ├── build.gradle
│   ├── consumer-rules.pro
│   └── src/
│       └── main/
│       └── java/com/atian10/logrecord/desktop/
│           ├── ConsoleStorage.java          # 控制台输出
│           ├── jdbc/
│           │   ├── JdbcHelper.java          # JDBC 工具
│           │   ├── LogTableSchema.java      # 表结构定义
│           │   └── JdbcMigrations.java      # 迁移
│           ├── JdbcStorage.java             # JDBC 实现 IStorage
│           ├── JdbcExceptionStorage.java    # JDBC 实现异常存储
│           └── DesktopLogInit.java          # 桌面初始化入口
│                                           # （文件写入能力由 log-core 的 Exporter 实现，无独立 FileWriteUtil）
│
└── log-sample/                                  # 使用示例
    ├── build.gradle
    └── src/
        ├── android/                             # Android 示例
        │   └── SampleApp.java
        ├── desktop/                             # 桌面示例
        │   └── DesktopSample.java
        └── server/                              # 服务器示例
            └── ServerSample.java
```

---

## 四、核心接口设计

### 4.1 写入接口 ILogger

```java
public interface ILogger {
    // 基础级别写入
    void d(String tag, String message);
    void i(String tag, String message);
    void w(String tag, String message);
    void e(String tag, String message);
    void f(String tag, String message);

    // 携带异常（便捷方法，内部拆为两条独立记录）
    void e(String tag, String message, Throwable throwable);
    void f(String tag, String message, Throwable throwable);

    // 完整参数写入
    void log(LogLevel level, String type, String tag, String message);
    void log(LogLevel level, String type, String tag, String message, Map<String, String> userFields);
    void log(LogLevel level, String type, String tag, String message, Throwable throwable);
    void log(LogLevel level, String type, String tag, String message, Map<String, String> userFields, Throwable throwable);

    // 异常独立写入
    void recordException(Throwable throwable, String tag);

    // 控制方法
    void flush();
    void shutdown();
}
```

### 4.2 存储接口 IStorage

```java
public interface IStorage {
    // 写入日志
    void write(LogRecord record);

    // 批量写入
    void writeBatch(List<LogRecord> records);

    // 查询日志
    List<LogRecord> query(LogQuery query);

    // 聚合统计
    LogStatistics statistics(LogQuery query);

    // 清理
    int clean(CleanPolicy policy);
    int cleanBefore(long timestamp);
    int cleanByCount(int keepCount);

    // 数据库容量信息
    long getRecordCount();
    long getDbSizeBytes();

    // 查询符合条件的记录数（用于导出分页）
    long count(LogQuery query);
}
```

### 4.3 异常存储接口 IExceptionStorage（独立）

```java
public interface IExceptionStorage {
    // 写入异常
    void write(ExceptionRecord record);

    // 批量写入
    void writeBatch(List<ExceptionRecord> records);

    // 查询异常
    List<ExceptionRecord> query(ExceptionQuery query);

    // 清理
    int clean(CleanPolicy policy);
    int cleanBefore(long timestamp);
    int cleanByCount(int keepCount);

    // 容量信息
    long getRecordCount();
    long getDbSizeBytes();

    // 查询符合条件的记录数（用于导出分页）
    long count(ExceptionQuery query);
}
```

### 4.4 格式化接口 IFormatter

```java
public interface IFormatter {
    // 格式化单条日志（用于导出）
    String format(LogRecord record, ExportFormat format);
    String format(ExceptionRecord record, ExportFormat format);
}
```

### 4.5 过滤器接口 ILogFilter

```java
public interface ILogFilter {
    // 是否允许写入
    boolean accept(LogRecord record);
}
```

### 4.6 引擎接口 ILoggerEngine

```java
public interface ILoggerEngine {
    // 提交日志到队列
    void submit(LogRecord record);

    // 提交异常到队列
    void submit(ExceptionRecord record);

    // 立即落盘
    void flush();

    // 关闭
    void shutdown();

    // 状态
    LogStatus getStatus();
}
```

### 4.7 LogManager 全局门面（核心入口）

LogManager 是业务方唯一接触的入口，聚合写入、查询、导出、清理、状态、配置更新全部能力。内部委托给 ILoggerEngine、IStorage、IExceptionStorage、Exporter 等组件。

```java
public final class LogManager implements ILogger {

    // ===== 初始化 =====
    // 平台无关入口：仅接收 LogConfig，由调用方先构建好 Storage 注入
    public static synchronized LogManager init(LogConfig config);
    public static LogManager get();                               // 获取单例
    public static boolean isInitialized();                        // 是否已初始化

    // ===== 写入（实现 ILogger，方法清单见 4.1）=====
    // d / i / w / e / f / log(...) / recordException(...) / e(tag,msg,throwable) / f(tag,msg,throwable)

    // ===== 查询 =====
    public List<LogRecord> queryLogs(LogQuery query);
    public List<ExceptionRecord> queryExceptions(ExceptionQuery query);
    public LogStatistics statistics(LogQuery query);

    // ===== 导出（委托给 Exporter，分页查询避免 OOM）=====
    // 编码从 LogConfig.getExportEncoding() 读取，调用方无需传入；返回导出条数
    public int exportLogs(LogQuery query, ExportFormat format, String filePath,
                          ExportCallback callback);
    public int exportExceptions(ExceptionQuery query, ExportFormat format, String filePath,
                                ExportCallback callback);

    // ===== 清理 =====
    public void cleanNow(CleanCallback callback);
    public int cleanBefore(long timestamp);
    public int cleanByCount(int keepCount);

    // ===== 状态 =====
    public LogStatus getStatus();
    public String getLastWarning();                               // 内部告警最近一条快照

    // ===== 配置更新 =====
    public LogConfigUpdater getConfigUpdater();

    // ===== 控制 =====
    public void flush();
    public void shutdown();                                       // 关闭后重置 instance，允许重新 init
}
```

> 注：Android 平台入口在 `AndroidLogInit.init(Context, LogConfig.Builder)`，桌面/服务器入口在 `DesktopLogInit.init(String dbPath, LogConfig.Builder)`，两者内部调用 `LogManager.init(LogConfig)`。

**设计要点**：
- LogManager 持有 ILoggerEngine、IStorage、IExceptionStorage、Exporter、CleanTask 的引用
- `init()` 时根据 LogConfig 创建引擎、Storage、Exporter
- 查询/统计方法委托给 IStorage（日志库场景通常只有一个 DB Storage）
- 导出方法委托给 Exporter，编码从 LogConfig 读取（配置归一化）
- `shutdown()` 重置静态 instance，允许业务方重新 init（用于进程内重启日志库场景）
- `get()` 未初始化时返回降级实例（输出到 System.out，不抛异常）

### 4.8 辅助类型定义

#### QueueFullPolicy（队列满策略枚举）

```java
public enum QueueFullPolicy {
    DROP_OLDEST,    // 丢弃最旧日志（默认）
    DROP_NEWEST,    // 丢弃最新日志（当前提交的）
    BLOCK           // 阻塞等待（慎用，可能阻塞业务线程）
}
```

#### LogStatus（库状态类，非枚举）

```java
public final class LogStatus {
    private final State state;          // NORMAL / DEGRADED / ERROR / SHUTDOWN
    private final String message;       // 状态描述
    private final long warningCount;    // 内部告警计数

    public enum State { NORMAL, DEGRADED, ERROR, SHUTDOWN }

    public boolean isNormal();
    public boolean isDegraded();
    public boolean isError();
    public boolean isShutdown();
    public String getMessage();
    public long getWarningCount();
}
```

#### LogEntry（队列包装类，区分日志/异常）

```java
public final class LogEntry {
    private final int type;             // 0=LogRecord, 1=ExceptionRecord
    private final LogRecord logRecord;
    private final ExceptionRecord exceptionRecord;

    public static LogEntry of(LogRecord record);
    public static LogEntry of(ExceptionRecord record);

    public boolean isLogRecord();
    public boolean isExceptionRecord();
    public LogRecord getLogRecord();
    public ExceptionRecord getExceptionRecord();
}
```

**批处理分类机制**（解决 P11 防凑数钩）：
- 队列类型为 `BlockingQueue<LogEntry>`
- 工作线程取出 LogEntry 后按 `isLogRecord()` 分类
- 攒批时分别收集到 `List<LogRecord>` 和 `List<ExceptionRecord>`
- 达到批处理阈值或超时后，分别调用 `IStorage.writeBatch()` 和 `IExceptionStorage.writeBatch()`

---

## 五、配置设计

### 5.1 LogConfig

```java
public final class LogConfig {
    // ===== 静态配置（初始化后建议不修改） =====
    private final IStorage storage;                  // 单一存储（不是 List，平台适配层用装饰器聚合多通道）
    private final IExceptionStorage exceptionStorage;
    private final int queueCapacity;                 // 异步队列容量
    private final QueueFullPolicy queueFullPolicy;
    private final int batchSize;                     // 批量写入阈值
    private final long batchIntervalMillis;          // 批量写入周期

    // ===== 动态配置（可运行时修改） =====
    private final boolean consoleEnabled;            // 控制台开关（平台适配层装饰器内部读取，LogManager 自身强制 false）
    private final boolean captureMethodLine;         // 方法名/行号捕获（默认 false）
    private final String versionTag;                 // 版本标签（语义由业务方自定义，可空）
    private final IFormatter formatter;              // 格式化器（null 表示用 DefaultFormatter）
    private final ILogFilter logFilter;              // 日志过滤器（单数，非 List）
    private final CleanPolicy cleanPolicy;
    private final CleanPolicy exceptionCleanPolicy;  // 异常表独立清理策略
    private final ExportEncoding exportEncoding;     // 导出编码

    // Builder 模式
    public static class Builder {
        public Builder storage(IStorage storage);
        public Builder exceptionStorage(IExceptionStorage storage);
        public Builder queueCapacity(int capacity);
        public Builder queueFullPolicy(QueueFullPolicy policy);
        public Builder batchSize(int size);
        public Builder batchIntervalMillis(long millis);
        public Builder consoleEnabled(boolean enabled);
        public Builder captureMethodLine(boolean capture);
        public Builder versionTag(String tag);
        public Builder formatter(IFormatter formatter);
        public Builder logFilter(ILogFilter filter);
        public Builder cleanPolicy(CleanPolicy policy);
        public Builder exceptionCleanPolicy(CleanPolicy policy);
        public Builder exportEncoding(ExportEncoding encoding);
        public LogConfig build();
    }

    // 基于已有配置创建 Builder（用于运行时修改时拷贝）
    public static Builder builderFrom(LogConfig source);
}
```

> 实现说明：
> - 平台适配层（AndroidLogInit/DesktopLogInit）会自动注入 `storage`/`exceptionStorage`，业务方在 Builder 中无需手动设置
> - 多通道输出（DB + Console）通过**装饰器模式**实现（LogcatStorage/ConsoleStorage 包装 DB Storage），而非 `List<IStorage>`
> - LogConfig 为不可变对象，运行时修改通过 `LogConfigUpdater` 以 `AtomicReference<LogConfig>` CAS 整体替换实现并发可见性

### 5.2 CleanPolicy

```java
public final class CleanPolicy {
    private final boolean enabled;
    private final int keepDays;              // 保留天数（0=不按天清理）
    private final long maxDbSizeMB;          // 数据库大小上限（0=不限）
    private final long maxRecordCount;       // 记录数上限（0=不限）
    private final int cleanIntervalHours;    // 清理周期（小时）

    // 注意：异常表独立策略通过 LogConfig.exceptionCleanPolicy 单独配置，
    //       本类不内嵌 exceptionPolicy 字段，避免递归类型。

    public static class Builder {
        public Builder enable(boolean enable);
        public Builder keepDays(int days);
        public Builder maxDbSizeMB(long sizeMB);
        public Builder maxRecordCount(long count);
        public Builder cleanIntervalHours(int hours);
        public CleanPolicy build();
    }
}
```

### 5.3 LogConfigUpdater（运行时动态修改）

```java
public final class LogConfigUpdater {
    // 持有 AtomicReference<LogConfig>，通过 CAS 整体替换实现线程安全
    public LogConfigUpdater(LogConfig initialConfig);
    public LogConfig get();

    // 各 update 方法内部循环 CAS 替换，无需手动 apply()
    public void updateConsoleEnabled(boolean consoleEnabled);
    public void updateCaptureMethodLine(boolean captureMethodLine);
    public void updateVersionTag(String versionTag);
    public void updateFormatter(IFormatter formatter);
    public void updateLogFilter(ILogFilter logFilter);
    public void updateCleanPolicy(CleanPolicy cleanPolicy);
    public void updateExceptionCleanPolicy(CleanPolicy exceptionCleanPolicy);
    public void updateExportEncoding(ExportEncoding exportEncoding);

    // 整体替换（高级用法）
    public void set(LogConfig newConfig);
}
```

**可修改项**：versionTag、captureMethodLine、consoleEnabled、formatter、logFilter、cleanPolicy、exceptionCleanPolicy、exportEncoding

**不可修改项**：storage、exceptionStorage、queueCapacity、queueFullPolicy、batchSize、batchIntervalMillis（需重新 init）

> 修改 cleanPolicy / exceptionCleanPolicy 后，需调用 `LogManager.get().refreshCleanPolicies()` 同步到定时清理任务（CleanTask）。

---

## 六、异步引擎设计

### 6.1 AsyncLoggerEngine

```java
public class AsyncLoggerEngine implements ILoggerEngine {
    private final BlockingQueue<LogEntry> queue;       // LogEntry 包装类（见 4.8）
    private final Thread workerThread;
    private final IStorage storage;                    // 单一 Storage（装饰器模式聚合多通道）
    private final IExceptionStorage exceptionStorage;
    private volatile boolean running = true;

    // 引擎持有的静态配置（运行时不可变）
    private final int queueCapacity;
    private final QueueFullPolicy queueFullPolicy;
    private final int batchSize;
    private final long batchIntervalMillis;
}
```

**并发可见性说明**（解决 P12 防凑数钩）：
- LogConfig 本身为不可变对象（`final class` + `final` 字段），`LogConfigUpdater` 持有 `AtomicReference<LogConfig>`，通过 CAS 整体替换保证并发可见性，避免字段级 `volatile` 的中间态问题。
- 工作线程在构造 `LogRecord` 时通过 `LogConfigUpdater.get()` 读取最新配置的 `versionTag`、`captureMethodLine` 等，实现运行时动态修改生效。
- `captureMethodLine`、`consoleEnabled`、`versionTag` 等不单独用 `volatile`，而是随 LogConfig 整体替换可见（不可变对象 + AtomicReference 模式）。

### 6.2 工作流程

```
业务线程                队列              工作线程              Storage
   │                    │                   │                    │
   │── submit(record) ──▶│                   │                    │
   │                    │── take() ─────────▶│                    │
   │                    │                   │── 攒批 (100条/1秒) │
   │                    │                   │── writeBatch() ─────▶│
   │                    │                   │                    │── 写入数据库
   │                    │                   │                    │
   │── flush() ─────────────────────────────▶│                   │
   │                    │                   │── 立即写批次 ──────▶│
   │                    │                   │                    │
```

### 6.3 异常隔离

- 每个 Storage 独立 try-catch
- 单个 Storage 异常不影响其他 Storage
- 异常记录到内部告警计数器
- 不向业务线程传播异常

---

## 七、查询设计

### 7.1 LogQuery

```java
public final class LogQuery {
    private LogLevel level;
    private String type;
    private String tag;
    private String keyword;
    private Long fromTime;
    private Long toTime;
    private Map<String, String> userFields;
    private String versionTag;
    private OrderBy orderBy;
    private int offset;
    private int limit;

    public static Builder builder();
}
```

### 7.2 ExceptionQuery

```java
public final class ExceptionQuery {
    private String tag;
    private String exceptionClass;
    private String keyword;
    private Long fromTime;
    private Long toTime;
    private OrderBy orderBy;
    private int offset;
    private int limit;

    public static Builder builder();
}
```

### 7.3 查询性能优化

| 查询字段 | 索引 | 说明 |
|---------|------|------|
| timestamp | ✅ | 时间范围查询走索引 |
| level | ✅ | 级别查询走索引 |
| type | ✅ | 类型查询走索引 |
| tag | ✅ | Tag 查询走索引 |
| version_tag | ✅ | 版本标签查询走索引 |
| user_fields | ❌ | JSON 存储走 json_extract 全表扫描 |
| message(keyword) | ❌ | LIKE 全表扫描 |

---

## 八、导出设计

### 8.1 导出器架构

Exporter 由 LogManager 在 `init()` 时创建，持有 IStorage 和 IExceptionStorage 引用。业务方不直接创建 Exporter，通过 `LogManager.get().exportLogs(...)` / `exportExceptions(...)` 调用。

```java
public class Exporter {
    private final IStorage storage;
    private final IExceptionStorage exceptionStorage;
    private final IFormatter formatter;

    // 由 LogManager 创建，业务方不直接构造
    public Exporter(IStorage storage, IExceptionStorage exceptionStorage,
                    IFormatter formatter);

    // 同步导出日志（分页查询，避免 OOM）；返回导出条数
    // encoding 从 LogConfig.getExportEncoding() 读取
    public int exportLogs(LogQuery query, ExportFormat format,
                          ExportEncoding encoding, String filePath,
                          ExportCallback callback);

    // 同步导出异常（分页查询，避免 OOM）；返回导出条数
    public int exportExceptions(ExceptionQuery query, ExportFormat format,
                                ExportEncoding encoding, String filePath,
                                ExportCallback callback);
}
```

> 注：Exporter 采用同步执行，由调用方决定是否异步（如包在业务线程中）。这样设计避免库内部维护线程池，降低复杂度；分页查询机制保证内存安全。

### 8.2 导出分页查询机制（防 OOM）

导出大量日志时采用分页查询，避免一次性加载到内存导致 OOM：

```
1. 根据 LogQuery 计算总条数（storage.count(query)）
2. 每页查询 PAGE_SIZE = 1000 条
3. 打开输出文件（BufferedWriter，指定编码）
4. 循环：query.setOffset(page * PAGE_SIZE).setLimit(PAGE_SIZE)
   → 查询一页 → formatter 格式化 → 写入文件 → 释放引用
5. 全部写完，关闭文件
6. 回调 ExportCallback.onSuccess(filePath, totalCount)
```

**内存控制**：任意时刻内存中只持有 1 页（1000 条）日志，不累积。

### 8.3 三种导出格式

#### TXT 格式

```
2026-07-15 10:30:45.123  ERROR  NETWORK  [LoginActivity]  Thread-1  用户登录失败
  at UserService.login(UserService.java:45)
  at LoginActivity.onClick(LoginActivity.java:78)
---
2026-07-15 10:31:02.456  INFO  UI  [MainActivity]  Thread-1  页面加载完成
```

#### JSON 格式

```json
[
  {
    "timestamp": 1721015445123,
    "level": "ERROR",
    "type": "NETWORK",
    "tag": "LoginActivity",
    "threadName": "Thread-1",
    "message": "用户登录失败",
    "userFields": {"userId": "10086", "errorCode": "AUTH_FAIL"},
    "versionTag": "1.2.3",
    "hasException": true
  }
]
```

#### CSV 格式

```csv
timestamp,level,type,tag,thread,message,user_fields,version_tag,has_exception
1721015445123,ERROR,NETWORK,LoginActivity,Thread-1,用户登录失败,"{""userId"":""10086"",""errorCode"":""AUTH_FAIL""}",1.2.3,1
```

---

## 九、清理设计

### 9.1 清理触发时机

| 触发方式 | 说明 |
|---------|------|
| 定时触发 | 后台 ScheduledExecutorService 定时执行（默认每天一次） |
| 写入前检查 | 每次写入前检查 maxDbSizeMB / maxRecordCount，超限触发同步清理 |
| 手动触发 | 业务方调用 `cleanNow()` |

### 9.2 清理流程

```
1. 检查 CleanPolicy.enabled
2. 按天数清理：DELETE FROM log_record WHERE timestamp < (now - keepDays)
3. 按容量清理：若 dbSize > maxDbSizeMB，删除最旧记录直到达标
4. 按数量清理：若 count > maxRecordCount，删除最旧记录直到达标
5. 异常表独立清理（按 exceptionPolicy）
6. VACUUM 回收空间（可选）
7. 回调通知清理结果
```

---

## 十、平台适配设计

### 10.1 Android 适配（log-android）

| 组件 | 实现 |
|------|------|
| 数据库 | Room（LogDatabase + LogDao + ExceptionDao） |
| 控制台 | LogcatStorage（android.util.Log） |
| 初始化 | AndroidLogInit.init(context, config) |
| 数据库位置 | context.getDatabasePath("log_record.db") |
| 迁移 | LogDatabaseMigrations（Room Migration） |

### 10.2 桌面/服务器适配（log-desktop）

| 组件 | 实现 |
|------|------|
| 数据库 | JDBC SQLite（JdbcHelper + JdbcStorage） |
| 控制台 | ConsoleStorage（System.out/err） |
| 初始化 | DesktopLogInit.init(config) |
| 数据库位置 | 业务方指定路径 |
| 迁移 | JdbcMigrations（ALTER TABLE） |
| 连接管理 | 单例 Connection + WAL 模式 |

### 10.3 WAL 模式设置

```java
// JDBC SQLite 初始化时设置
Statement stmt = connection.createStatement();
stmt.execute("PRAGMA journal_mode=WAL");
stmt.execute("PRAGMA synchronous=NORMAL");
stmt.close();
```

---

## 十一、Gradle 构建设计

### 11.1 根 build.gradle

```groovy
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
```

### 11.2 settings.gradle

```groovy
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':log-core'
include ':log-android'
include ':log-desktop'
include ':log-sample'
include ':log-sample-android'
```

### 11.3 log-core/build.gradle

```groovy
apply plugin: 'java-library'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    // JSON 处理（轻量库）
    implementation 'com.google.code.gson:gson:2.10.1'
    testImplementation 'junit:junit:4.13.2'
}
```

### 11.4 log-android/build.gradle

```groovy
apply plugin: 'com.android.library'

android {
    compileSdk 33
    defaultConfig {
        minSdk 21
        targetSdk 33
    }
}

dependencies {
    api project(':log-core')
    // Room
    implementation 'androidx.room:room-runtime:2.5.2'
    annotationProcessor 'androidx.room:room-compiler:2.5.2'
}
```

### 11.5 log-desktop/build.gradle

```groovy
apply plugin: 'java-library'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    api project(':log-core')
    // SQLite JDBC 驱动
    implementation 'org.xerial:sqlite-jdbc:3.42.0.0'
}
```

---

## 十二、ProGuard/R8 配套

### 12.1 log-core/consumer-rules.pro

```proguard
# 核心数据模型 keep（供反射调用）
-keep class com.atian10.logrecord.core.model.** { *; }

# 核心接口 keep
-keep class com.atian10.logrecord.core.IStorage { *; }
-keep class com.atian10.logrecord.core.IFormatter { *; }
-keep class com.atian10.logrecord.core.ILogFilter { *; }

# 配置类 keep
-keep class com.atian10.logrecord.core.config.** { *; }
```

### 12.2 log-android/consumer-rules.pro

```proguard
# Room Entity keep
-keep class com.atian10.logrecord.android.room.** { *; }

# Gson 模型 keep（如有 JSON 序列化）
-keepattributes Signature
-keepattributes *Annotation*
```

### 12.3 log-desktop/consumer-rules.pro

```proguard
# JDBC 驱动 keep
-keep class org.sqlite.** { *; }
-keep class com.atian10.logrecord.desktop.jdbc.** { *; }
```

---

## 十三、包名规范

```
com.atian10.logrecord
├── core                    # log-core 模块
│   ├── model               # 数据模型
│   ├── config              # 配置
│   ├── engine              # 引擎
│   ├── query               # 查询
│   ├── export              # 导出
│   ├── formatter           # 格式化
│   ├── clean               # 清理
│   └── util                # 工具
├── android                 # log-android 模块
│   ├── room                # Room 实现
│   └── ...
└── desktop                 # log-desktop 模块
    ├── jdbc                # JDBC 实现
    └── ...
```

---

## 十四、技术选型汇总

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 构建工具 | Gradle | 7.6+ | 多模块构建 |
| Java 版本 | Java SE | 1.8 | 全平台兼容 |
| JSON 处理 | Gson | 2.10.1 | user_fields 序列化 |
| Android 数据库 | Room | 2.5.2 | Android ORM |
| 桌面数据库 | SQLite JDBC | 3.42.0.0 | 桌面/服务器 |
| 单元测试 | JUnit | 4.13.2 | log-core 测试 |
| Android compileSdk | 33 | - | Android 编译版本 |
| Android minSdk | 21 | - | 最低支持版本 |

---

## 十五、版本管理

### 15.1 版本号规范

采用语义化版本：`MAJOR.MINOR.PATCH`

- MAJOR：不兼容的 API 修改
- MINOR：向下兼容的功能新增
- PATCH：向下兼容的问题修复

### 15.2 数据库版本管理

| 数据库版本 | 表结构变更 | 迁移方式 |
|-----------|-----------|---------|
| 1 | 初始版本（log_record + exception_table） | 初始创建 |
| 2+ | 后续新增字段/表 | Room Migration / ALTER TABLE |

---

## 文档变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-07-15 | 初始版本，确认架构设计 |
| v1.1 | 2026-07-15 | 自查修复：新增 LogManager 门面章节；补充 QueueFullPolicy/LogStatus/LogEntry 定义；LogConfig 加 consoleEnabled/exceptionCleanPolicy；CleanPolicy 移除递归 exceptionPolicy；IExceptionStorage 补充方法；删除 ICleanable/AndroidFileStorage；Exporter 明确初始化与分页查询；补充并发可见性说明；log-desktop 补充 FileWriteUtil |
| v1.2 | 2026-07-16 | 同步实际实现 API：LogConfig 字段更名与新增；LogConfigUpdater 改为 CAS 模式；AsyncLoggerEngine 并发可见性改为 AtomicReference 整体替换；LogManager 导出方法签名改为 exportLogs/exportExceptions（编码从 LogConfig 读取，返回导出条数） |
| v1.3 | 2026-07-16 | 复查同步：4.7 节 LogManager 签名同步为代码实际（query→queryLogs、updater→getConfigUpdater、init 移除 Context 重载、export/exportExceptions 签名修正、新增 cleanBefore/cleanByCount/getLastWarning/isInitialized/shutdown 重置说明）；8.1 节 Exporter 改为同步实现（删除独立线程池描述，方法名 exportAsync→exportLogs）；删除 L184 不存在的 FileWriteUtil.java 条目 |
| v1.4 | 2026-07-20 | 低优先级修复：LogStatus.State 新增 SHUTDOWN 枚举值（区分"工作线程崩溃 ERROR"与"引擎正常关闭 SHUTDOWN"）；新增 LikeEscapeUtil 工具类，RoomStorage/JdbcStorage/RoomExceptionStorage/JdbcExceptionStorage 的 LIKE 查询添加 ESCAPE '\\' + 转义 %/_/\ 字符；LogQuery/ExceptionQuery.Builder.orderBy 传 null 时保持默认 DESC（不覆盖）；AndroidLogInit PRAGMA 异常输出到 logcat；sample 模块显式声明 log-core 依赖 |
